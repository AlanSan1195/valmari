<section
  class="h-full w-full flex items-center content-center justify-center md:items-center md:justify-center mx-4 md:mx-0 "
  aria-label="Sección principal de Valmarí"
>
  <div
    id="hero-container"
    class=" max-w-xl  sm:max-w-2xl md:max-w-7xl  h-full flex flex-col md:flex-row justify-center mx-auto md:gap-x-8"
  >
    <!-- hero title  -->
    <div
      class="flex-col w-auto h-full items-center content-center   z-20 
       "
    >
      <p
        class="font-mango  text-2xl md:text-xl lg:text-3xl text-center  md:rounded-lg w-full mx-auto"
      >
        <span class="text-primary">...</span> se van a juntar <span
          class="text-primary">?</span
        >
      </p>

      <h1 class="font-primary edit text-[120px] xl:text-[150px] text-center">
        La botana primero
      </h1>
    </div>

    <div id="logo-hero-sm" class="md:hidden scale-70">
      <img
        id="linea1"
        src="/linea1.svg"
        alt="linea 1"
        class="absolute translate-x-2 scale-105"
      />

      <img
        id="basic-logo"
        src="/basic.svg"
        alt="logo de valmari"
        class="md:hidden"
      />
      <img
        id="linea2"
        src="/linea2.svg"
        alt="linea 2"
        class="absolute top-0 scale-110 translate-x-5"
      />
    </div>
    <div
      id="logo-hero-md"
      class="hidden opacity-0 h-full w-full md:block md:opacity-100 content-center relative  scale-100 md:scale-90 "
    >
      <div id="logo-md" class=" ">
        <div class="relative flex justify-center items-center">
          <img
            id="basic-logo"
            src="/basic.svg"
            alt="logo de valmari"
            class=" scale-90 md:scale-130  "
          />
          <img
            id="linea2"
            src="/linea2.svg"
            alt="linea 2"
            class="absolute top-0 left-0 translate-x-8 -translate-y-0  md:translate-x-7 md:-translate-y-6 scale-100 md:scale-130"
          /> 
          <div id="blur-logo-md" class="absolute inset-0  scale-100 md:scale-110 ">
            <img
              id="linea1"
              src="/linea1.svg"
              alt="linea 1"
              class="absolute translate-x-4 -translate-y-4 scale-90 md:scale-110"
            />
            <img
              id="linea1"
              src="/linea1.svg"
              alt="linea 1"
              class="absolute top-0 left-0 scale-90 md:scale-110 pulse blur-sm"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .edit {
    line-height: 0.8;
  }

  @keyframes move-line-x {
    to {
      transform: translateX(0);
    }
    from {
      transform: translateX(10px);
    }
  }
  @keyframes move-line-y {
    to {
      transform: scale(1) translateY(0);
    }
    from {
      transform: scale(1.02) translateY(0);
    }
  }

  @keyframes change-logo {
    0% {
      scale: 1.4;
    }
    50% {
      scale: 0.5;
    }

    100% {
      scale: 0;
    }
  }

  @keyframes pulse {
    0% {
      opacity: 0.5;
    }
    50% {
      opacity: 0.95;
    }
    100% {
      opacity: 0.9;
    }
  }

  .pulse {
    animation: pulse 3s infinite;
  }
  #prueba-md {
    animation: change-logo 10s linear;
    animation-delay: 800ms;
    animation-timeline: scroll(block);
  }

  #linea1 {
    animation: move-line-x 3s ease-in infinite alternate;
  }
  #linea2 {
    animation: move-line-y 3s ease-in infinite alternate;
    animation-delay: 20ms;
  }
</style>
